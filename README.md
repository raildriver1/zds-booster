# ZDS-Booster

Модифицированный движок для ZDSimulator с расширенными возможностями отображения информации, управления камерой и настройки визуальных эффектов.

## 🚀 Возможности

### 🎥 Система свободной камеры (Freecam)
- **Включение/выключение**: `Alt + Numpad 1` (включить), `Numpad 1` (выключить)
- **Управление движением**: 
  - `W/S` - вперед/назад
  - `A/D` - влево/вправо  
  - `Пробел` - вверх
  - `Shift` - ускорение
- **Управление камерой**: Стрелки для поворота
- **Автоматическая активация** через конфиг файл

### 📊 Расширенная информация в кабине (только ЧС7)
- **Основные данные (BGSD)**: время, дата, координаты, давление, ускорение
- **Система скорости и лимитов (SAUT)**: цифровые индикаторы скорости и допустимых ограничений
- **Ступени тяги (STUPEN)**: отображение текущих ступеней
- **Информация о маршруте**: текущая и следующая станции

### 🚦 Система светофоров АЛС-ЕН (только ЧС7)
- **Команда активации**: `137` → `ENTER` → выбор частоты (1-3) → `ENTER`
- **Визуальная индикация**: цветные блоки согласно показаниям светофоров
- **Двойная система отображения**: КЛУБ и БИЛ ПОМЕ
- **Автоматический анализ** последовательности сигналов
- **⚠️ ВАЖНО**: HookKLUB работает только на локомотиве ЧС7 (тип 822)

### 🌅 Система смены текстур день/ночь
- **Автоматическая смена** в зависимости от времени суток и сезона
- **Поддержка зимы и лета** с разными временными интервалами
- **Кастомные текстуры** для кабины, пульта и других элементов

## 📁 Структура файлов

```
ZDSimulator/
├── DGLEngine.dll          # Основной модифицированный движок
├── zdbooster.cfg          # Главный конфиг (автосоздается)
├── data/
│   └── [тип_локомотива]/
│       └── [номер]/
│           ├── loc/                    # Кастомные модели
│           │   ├── klub_bil_v.dmd     # Модель КЛУБ
│           │   ├── klub_bil.bmp       # Текстура КЛУБ
│           │   ├── kolpara_*.dmd      # Модели колпаков
│           │   ├── klub_ls_*.dmd      # Модели светофоров АЛС
│           │   ├── strelka_skor.dmd   # Стрелка скорости
│           │   ├── pisec1.dmd         # Писец 1
│           │   ├── pisec2.dmd         # Писец 2
│           │   └── ...
│           ├── night/                  # Ночные текстуры
│           │   ├── cab.bmp
│           │   ├── pult.bmp
│           │   └── klub_bil.bmp
│           └── raildriver/             # Конфиги отображения
│               ├── booster.txt
│               └── arrow_usavpp.txt
└── routes/
    └── [название_маршрута]/
        └── textures/
            ├── sky_sunriseDawn.bmp      # Предрассветные сумерки
            ├── sky_sunsetTwilight.bmp   # Закатные сумерки
            ├── sky_*_snow.bmp           # Зимние варианты
            └── ...
```

## ⚙️ Конфигурация

### Главный конфиг `zdbooster.cfg`
```ini
# Включение/выключение систем (1 = вкл, 0 = выкл)
freecam: 1              # Свободная камера
main_camera: 1          # Настройка основной камеры  
max_distance: 1         # Увеличенная дистанция видимости
newsky: 1              # Новая система неба

# Параметры фрикама
basespeed: 0.5         # Базовая скорость
fastspeed: 1.0         # Быстрая скорость (Shift)
turnspeed: 1.5         # Скорость поворота

# Параметры камеры
stepforward: 0.1       # Шаг движения (Shift + стрелки)

# Дистанция видимости
maxvisibledistance: 1200
```

### Конфиг отображения `raildriver/booster.txt`
```ini
# Настройки отображения элементов
saut: 0     # Скорость и лимиты (желтые/красные цифры)
bgsd: 1     # Основные данные (время, координаты и т.д.)
stupen: 1   # Ступени тяги
```

### Конфиг стрелки спидометра `raildriver/arrow_usavpp.txt`
```ini
# Позиция и параметры стрелки
arrowX: 0.895
arrowY: 7.45  
arrowZ: 3.64
arrowScale: 1.61
koef: 1.63              # Коэффициент скорости
updateInterval: 500     # Интервал обновления (мс)
```

> **⚠️ Важно**: Система HookKLUB (отображение информации в кабине и АЛС-ЕН) работает только на ЧС7. Остальные функции (фрикам, текстуры день/ночь, новое небо) работают на всех поддерживаемых локомотивах.

## 🛠️ Установка

1. **Резервная копия**: Сохраните оригинальный `DGLEngine.dll`
   ```bash
   copy DGLEngine.dll DGLEngine_backup.dll
   ```
2. **Замена файла**: Скопируйте модифицированный `DGLEngine.dll` в папку игры
3. **Настройка**: При первом запуске автоматически создастся `zdbooster.cfg`
4. **Кастомизация**: Добавьте кастомные модели и текстуры по желанию

## 🎮 Управление

### Основное
- `Insert` - Меню настроек (если доступно)
- `Alt + Numpad 1` - Включить фрикам
- `Numpad 1` - Выключить фрикам
- `137` + `Enter` - Система АЛС-ЕН (только ЧС7)
- `10` + `Enter` - Команда raildriver

### В режиме фрикама
- `W/A/S/D` - Движение
- `Пробел` - Подъем
- `Shift` - Ускорение

### Основная камера
- `Shift + Стрелка вверх/вниз` - Точная настройка вперед/назад в кабине

## 🔧 HookKLUB (только ЧС7)

### Функции HookKLUB
```pascal
procedure HookKLUB(
  x: Single;     // Поворот Z
  y: Single;     // Позиция Z  
  z: Single;     // Позиция Y
  AngZ: Single   // Позиция X
); stdcall; export;
```

### Адреса памяти (ЧС7)
- `$00400000 + $4F8D93C` - Тип локомотива
- `$00400000 + $34988C` - Состояние интерфейса
- `$9004398` - Yaw камеры
- `$900439C` - Pitch камеры
- `$9008028` - X позиция
- `$900802C` - Y позиция  
- `$9008030` - Z позиция

### Патчинг
```pascal
// Основной вызов патча
CallAddress := $00400000 + $277938;
NewOffset := Integer(HookAddr) - Integer(CallAddress + 5);
```

## 🐛 Диагностика

### Лог файлы
- `EngineLog.txt` - Основной лог движка
- Содержит информацию о:
  - Загрузке моделей и текстур
  - Активации систем
  - Ошибках инициализации
  - Типе локомотива

### Типичные проблемы

1. **HookKLUB не работает**
   ```
   Проверьте лог: "Hook activated for locomotive type 822 (CS7)"
   Если нет - используете не ЧС7
   ```

2. **Фрикам не активируется**
   ```
   Проверьте: freecam: 1 в zdbooster.cfg
   Лог: "Freecam initialized for locomotive type"
   ```

3. **Не загружаются модели**
   ```
   Проверьте структуру: data/chs7/[номер]/loc/
   Лог: "Модель [название] загружена, ID: [число]"
   ```

4. **Не меняются текстуры день/ночь**
   ```
   Проверьте: newsky: 1 в zdbooster.cfg
   Наличие папки night/ с файлами
   ```

### Отладочная информация
```ini
# В коде включена отладка
debug = True;

# Логирование в реальном времени
AddToLogFile(EngineLog, 'Diagnostic message');
```

## 📝 История изменений

### v1.0
- ✅ Система свободной камеры с конфигурацией
- ✅ HookKLUB для ЧС7 с отображением информации в кабине
- ✅ Система светофоров АЛС-ЕН с визуальной индикацией
- ✅ Автоматическая смена текстур день/ночь
- ✅ Новая система неба с плавными переходами
- ✅ Поддержка кастомных моделей и текстур
- ✅ Гибкая система конфигурации через файлы

### Технические особенности
- Перехват вызовов через модификацию `WndProc`
- Патчинг памяти для активации функций
- Реал-тайм обновление конфигурации
- Безопасная обработка ошибок

## ⚠️ Важные замечания

- **Совместимость**: HookKLUB протестирован только на ЧС7 (тип 822)
- **Производительность**: При проблемах отключите ненужные системы в конфиге
- **Безопасность**: Всегда делайте резервные копии оригинальных файлов
- **Обновления**: Следите за обновлениями для поддержки новых локомотивов

## 🔍 Техническая информация

### Архитектура
```
DGLEngine.dll
├── EngineCore.pas     # Основной движок
├── DrawFunc3D.pas     # 3D функции + HookKLUB
├── Variables.pas      # Переменные
└── KlubData.pas      # Данные КЛУБ
```

### Методы патчинга
1. **VirtualProtect** - изменение защиты памяти
2. **Перехват вызовов** - модификация адресов функций  
3. **Инъекция кода** - добавление новых функций

### Требования
- Windows 7/10/11
- ZDSimulator
- OpenGL 1.x+
- Права администратора (для патчинга памяти)

## 📞 Поддержка

При возникновении проблем:
1. Проверьте `EngineLog.txt`
2. Убедитесь в правильности конфигурации
3. Для ЧС7: проверьте активацию HookKLUB в логе
4. Создайте Issue с описанием проблемы и логами
5. Напишите нам в группу https://vk.com/raildriver

---
*ZDBooster - модификация для энтузиастов железнодорожного моделирования*  
*HookKLUB работает только на ЧС7. Остальные функции доступны на всех локомотивах.*
